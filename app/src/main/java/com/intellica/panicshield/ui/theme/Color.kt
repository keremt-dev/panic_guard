package com.intellica.panicshield.ui.theme

import androidx.compose.ui.graphics.Color

// Surfaces — near-black with a faint cool cast; serious, OLED-friendly.
val BgBase = Color(0xFF0E0F13)
val BgElevated = Color(0xFF181A21)
val Outline = Color(0xFF2A2D37)

// Text
val TextPrimary = Color(0xFFF2F3F5)
val TextMuted = Color(0xFF9A9DA8)

// State accents
val AccentSafe = Color(0xFF34D399)   // armed & protected — calm emerald
val AccentWarn = Color(0xFFF59E0B)   // needs setup — amber
val AccentDanger = Color(0xFFF2424B) // panic / alarm — red (brand)
val AccentDangerDim = Color(0xFF7F1D1D)
